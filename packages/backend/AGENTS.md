# AGENTS.md

## Testing
- Run tests with `uv run pytest`

## Development

- Use structured logging via `sm`
  Example: `logger.info(sm("Processed file", path=file.path))`
- Do not use emojis in logs or otherwise unless explicitly stated
